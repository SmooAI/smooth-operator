# Guides — Overview

> [!note] Task-oriented how-tos. Each guide walks one concrete job end to end —
> running it locally, self-hosting, integrating into your app, building the
> showcase agent, writing a connector, and using the clients and React UIs.

| Page | What it covers |
| --- | --- |
| [[Getting Started]] | Clone, run the reference server, chat locally. |
| [[Self-Hosting]] | Run it on your own AWS or Kubernetes. |
| [[Integrating into an Existing App]] | BYO-JWT and trusted (proxied) auth, with mint snippets. |
| [[Build a Dev-Support Agent]] | The `examples/dev-support` recipe — ingest a repo, then chat. |
| [[Writing a Connector]] | Author a new knowledge source. |
| [[Using the Polyglot Clients]] | TS / Go / .NET / Python / Rust. |
| [[React Components and Custom UIs]] | The headless `useConversation` hook + themed `<SmoothChat>`. |

Back to [[Home]].
