server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # Single-page app: fall back to index.html so deep links resolve.
    location / {
        try_files $uri /index.html;
    }
}
