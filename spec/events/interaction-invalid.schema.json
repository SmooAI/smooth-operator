{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://smooth-agent.dev/spec/events/interaction-invalid.schema.json",
  "title": "InteractionInvalid",
  "description": "Event: `interaction_invalid`. Emitted when a `submit_interaction` action carried values that failed the kind's server-side validation. The turn REMAINS parked — the client should re-render the interaction card with the per-field errors and let the visitor resubmit. Mirrors `otp_invalid`: invalid input is a retryable state, never a terminal `error` event.",
  "type": "object",
  "required": ["type", "data"],
  "additionalProperties": false,
  "properties": {
    "type": {
      "type": "string",
      "const": "interaction_invalid",
      "description": "Event type discriminator."
    },
    "requestId": {
      "type": "string",
      "description": "Echoes the `requestId` of the parked turn (same correlation as the `interaction_required` event)."
    },
    "data": {
      "type": "object",
      "description": "Validation failure details.",
      "required": ["requestId", "data"],
      "additionalProperties": false,
      "properties": {
        "requestId": {
          "type": "string",
          "description": "The request ID this interaction belongs to."
        },
        "data": {
          "type": "object",
          "description": "Per-field validation errors.",
          "required": ["interactionId", "kind", "errors", "message"],
          "additionalProperties": false,
          "properties": {
            "interactionId": {
              "type": "string",
              "description": "The interaction instance the rejected submit targeted."
            },
            "kind": {
              "type": "string",
              "description": "The interaction kind (e.g. `identity_intake`)."
            },
            "errors": {
              "type": "array",
              "minItems": 1,
              "description": "One entry per failed field. `field` is a kind-specific field key (identity_intake: `name` | `email` | `phone`).",
              "items": {
                "type": "object",
                "required": ["field", "message"],
                "additionalProperties": false,
                "properties": {
                  "field": {
                    "type": "string",
                    "description": "The kind-specific field that failed validation."
                  },
                  "message": {
                    "type": "string",
                    "description": "Human-readable validation message for this field."
                  }
                }
              }
            },
            "message": {
              "type": "string",
              "description": "Human-readable summary suitable for a card-level error line."
            }
          }
        }
      }
    },
    "timestamp": {
      "type": "integer",
      "description": "Unix epoch milliseconds when the event was emitted."
    }
  }
}
