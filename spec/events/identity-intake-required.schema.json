{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://smooth-agent.dev/spec/events/identity-intake-required.schema.json",
  "title": "IdentityIntakeRequired",
  "description": "Event: `identity_intake_required`. Emitted mid-turn when the agent requests structured identity/lead intake (name / email / phone) and the session declared the `identity_form` capability at `create_conversation_session`. The turn is parked until the client replies with a `submit_identity_intake` action carrying the same `requestId` (values or `declined: true`). Sessions that did not declare `identity_form` never receive this event — the server degrades to conversational, turn-by-turn collection instead.",
  "type": "object",
  "required": ["type", "data"],
  "additionalProperties": false,
  "properties": {
    "type": {
      "type": "string",
      "const": "identity_intake_required",
      "description": "Event type discriminator."
    },
    "requestId": {
      "type": "string",
      "description": "Echoes the `requestId` from the originating `send_message` action. Must be included in the `submit_identity_intake` reply."
    },
    "data": {
      "type": "object",
      "description": "Intake prompt details.",
      "required": ["requestId", "data"],
      "additionalProperties": false,
      "properties": {
        "requestId": {
          "type": "string",
          "description": "The request ID this intake belongs to."
        },
        "data": {
          "type": "object",
          "description": "The fields the agent wants and why.",
          "required": ["fields", "reason"],
          "additionalProperties": false,
          "properties": {
            "fields": {
              "type": "array",
              "minItems": 1,
              "description": "The identity fields to collect, in display order.",
              "items": {
                "type": "object",
                "required": ["key", "required"],
                "additionalProperties": false,
                "properties": {
                  "key": {
                    "type": "string",
                    "enum": ["name", "email", "phone"],
                    "description": "Which identity field to collect."
                  },
                  "required": {
                    "type": "boolean",
                    "description": "Whether the visitor must provide this field to submit."
                  },
                  "label": {
                    "type": "string",
                    "description": "Optional display label overriding the client's default for this field."
                  }
                }
              }
            },
            "reason": {
              "type": "string",
              "description": "Human-readable reason the agent needs these details, suitable for the form header (e.g. `to send you the quote`)."
            }
          }
        }
      }
    },
    "timestamp": {
      "type": "integer",
      "description": "Unix epoch milliseconds when the event was emitted."
    }
  }
}
