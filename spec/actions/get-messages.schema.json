{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://smooth-agent.dev/spec/actions/get-messages.schema.json",
  "title": "GetMessages",
  "description": "Action: `get_conversation_messages`. Retrieves a page of messages from the conversation attached to the given session. Results are ordered newest-first. Page by feeding a response's `nextCursor` back as the next request's `cursor`. The server replies with an `immediate_response` event whose `data` matches the `Response` schema below.",

  "$defs": {
    "Request": {
      "title": "GetMessagesRequest",
      "description": "Fields sent by the client to page through conversation history.",
      "type": "object",
      "required": ["action", "sessionId"],
      "additionalProperties": false,
      "properties": {
        "action": {
          "type": "string",
          "const": "get_conversation_messages",
          "description": "Action discriminator."
        },
        "requestId": {
          "type": "string",
          "description": "Client-generated correlation ID echoed back on all related events."
        },
        "sessionId": {
          "type": "string",
          "format": "uuid",
          "description": "Session ID whose conversation messages to fetch."
        },
        "limit": {
          "type": "integer",
          "minimum": 1,
          "maximum": 100,
          "default": 50,
          "description": "Maximum number of messages to return per page. Must be 1–100; defaults to 50."
        },
        "cursor": {
          "type": "string",
          "description": "Opaque pagination cursor from a prior response's `nextCursor`. Returns only messages older than the one the cursor names. Treat it as opaque — its encoding is storage-defined (a message id today) and may change. Omit to start from the most recent message.\n\nDeliberately NOT a timestamp: two messages can share a timestamp at any precision the wire format keeps, so a `created_at < cursor` filter either drops or repeats the messages that collide. An id cursor identifies exactly one message and cannot."
        }
      }
    },

    "Response": {
      "title": "GetMessagesResponse",
      "description": "Data payload carried in the `immediate_response` event.",
      "type": "object",
      "required": ["messages", "hasMore"],
      "additionalProperties": false,
      "properties": {
        "messages": {
          "type": "array",
          "items": { "$ref": "#/$defs/ConversationMessage" },
          "description": "Ordered list of messages (newest-first up to `limit`)."
        },
        "nextCursor": {
          "type": ["string", "null"],
          "description": "Opaque cursor naming the oldest message in this page. Pass it as the next request's `cursor` to fetch the page before this one. Present (non-null) if and only if `hasMore` is true."
        },
        "hasMore": {
          "type": "boolean",
          "description": "True if more messages exist before the oldest message in this page — equivalently, if `nextCursor` is non-null."
        }
      }
    },

    "ConversationMessage": {
      "title": "ConversationMessage",
      "description": "A single message as returned on the wire. This is a subset of the full `Message` domain object — it omits server-only fields (metadataJson, analyticsJson) and includes abbreviated participant descriptors.",
      "type": "object",
      "required": ["id", "direction", "content", "createdAt"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "format": "uuid",
          "description": "Unique message ID."
        },
        "direction": {
          "type": "string",
          "enum": ["inbound", "outbound"],
          "description": "Message direction: `inbound` = from user, `outbound` = from agent."
        },
        "content": {
          "type": "object",
          "description": "Message payload. `text` contains the plain-text form; `structuredResponse` contains parsed agent output when present.",
          "additionalProperties": false,
          "properties": {
            "text": {
              "type": "string",
              "description": "Plain-text content."
            },
            "structuredResponse": {
              "description": "Structured agent response (shape depends on agent template).",
              "type": ["object", "null"],
              "additionalProperties": true
            }
          }
        },
        "from": {
          "type": "object",
          "description": "Abbreviated sender descriptor.",
          "required": ["id", "type"],
          "additionalProperties": false,
          "properties": {
            "id": {
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string"
            },
            "name": {
              "type": "string"
            }
          }
        },
        "to": {
          "type": "object",
          "description": "Abbreviated recipient descriptor.",
          "required": ["id", "type"],
          "additionalProperties": false,
          "properties": {
            "id": {
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string"
            },
            "name": {
              "type": "string"
            }
          }
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp when the message was created."
        }
      }
    }
  },

  "oneOf": [
    { "$ref": "#/$defs/Request" },
    { "$ref": "#/$defs/Response" }
  ]
}
