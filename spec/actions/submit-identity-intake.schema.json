{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://smooth-agent.dev/spec/actions/submit-identity-intake.schema.json",
  "title": "SubmitIdentityIntake",
  "description": "Action: `submit_identity_intake`. Sent in response to an `identity_intake_required` event. Resumes the parked turn with the visitor's identity values — or with `declined: true` when the visitor refuses (the agent handles the decline gracefully). The server validates values server-side (required fields, email format, E.164 phone); invalid values emit `identity_intake_invalid` and the turn stays parked for a resubmit. Valid values (or a decline) resume the turn: the agent's `request_identity_intake` tool returns the validated structured payload, and the server attaches the identity to the session (metadata `userName` / `contactEmail` / `contactPhone` — the same keys the OTP contact seam reads).",

  "$defs": {
    "Request": {
      "title": "SubmitIdentityIntakeRequest",
      "description": "Fields sent by the client to submit (or decline) identity intake.",
      "type": "object",
      "required": ["action", "sessionId", "requestId"],
      "additionalProperties": false,
      "properties": {
        "action": {
          "type": "string",
          "const": "submit_identity_intake",
          "description": "Action discriminator."
        },
        "requestId": {
          "type": "string",
          "description": "Must match the `requestId` from the `identity_intake_required` event being responded to."
        },
        "sessionId": {
          "type": "string",
          "format": "uuid",
          "description": "Session ID of the parked session."
        },
        "values": {
          "type": "object",
          "description": "The visitor's identity values. Required unless `declined` is true. Only the fields requested by the `identity_intake_required` event are meaningful; the server validates required-ness against that request.",
          "additionalProperties": false,
          "properties": {
            "name": {
              "type": "string",
              "description": "The visitor's display name."
            },
            "email": {
              "type": "string",
              "description": "The visitor's email address (validated server-side)."
            },
            "phone": {
              "type": "string",
              "description": "The visitor's phone number (normalized server-side to E.164)."
            }
          }
        },
        "declined": {
          "type": "boolean",
          "description": "True when the visitor refused to share their details. The turn resumes with a declined payload so the agent can proceed gracefully. When true, `values` is ignored."
        }
      }
    },

    "Response": {
      "title": "SubmitIdentityIntakeResponse",
      "description": "No dedicated response event for `submit_identity_intake`. Valid values (or a decline) are acked with an `immediate_response` and the parked turn resumes its normal streaming sequence. Invalid values emit `identity_intake_invalid` (the turn stays parked). This schema is provided for documentation completeness only.",
      "type": "object",
      "additionalProperties": true
    }
  },

  "oneOf": [
    { "$ref": "#/$defs/Request" },
    { "$ref": "#/$defs/Response" }
  ]
}
