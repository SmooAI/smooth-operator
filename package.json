{
    "name": "@smooai/smooth-operator-js",
    "version": "0.0.0",
    "private": true,
    "description": "Workspace root for the smooth-operator JavaScript/TypeScript packages. `typescript/` is the published npm SDK (client + React bindings + web-component widget, as subpath exports); `console/` is the private admin app.",
    "type": "module",
    "license": "MIT",
    "engines": {
        "node": ">=22"
    },
    "scripts": {
        "build": "pnpm -r build",
        "test": "pnpm -r test",
        "typecheck": "pnpm -r typecheck",
        "publish:dry": "pnpm --filter @smooai/smooth-operator publish --dry-run --no-git-checks"
    },
    "packageManager": "pnpm@9.15.0"
}
